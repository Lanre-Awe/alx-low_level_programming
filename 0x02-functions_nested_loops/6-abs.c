#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @int: type int 
 *
 * Return: the abolute value of int
 */

int _abs(int)
{
	if (int < 0)
	{
		return (int * -1);
	}
	else
	{
		return int;
	}
}
