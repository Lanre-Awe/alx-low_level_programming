#include "main.h"

/**
 * *_strchr - locates a character in a string
 * @s: type char
 * @c: type char
 */

char *_strchr(char *s, char c)
{
	int i;

	char *p;

	for (i = 0; i != '\0'; i++)
	{
		if (s[i] == 'c')
		{
			return (p);
		} else
		{
			return NULL;
		}
	}
}
